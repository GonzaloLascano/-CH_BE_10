DOCTYPE
  html(lang="es")
    head
      meta(charset="UTF-8")
      meta(name="viewport" content="width=device-width, initial-scale=1.0")
      
      link(href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous")
      title Desafio Clase 10
    body
      div(class="d-flex flex-column justify-content-around container-sm my-5 p-4 shadow rounded-3")
        h2(class="my-3 border-bottom") Tabla de Productos Listados 
        if products != 0
          table(class="table table-striped my-4")
            thead
              tr
                th(scope="col") ID
                th(scope="col") Producto
                th(scope="col") Precio
                th(scope="col") Thumbnail

            tbody
              each product in products
                tr
                  th(scope="row")= product.id
                  td= product.title
                  td= '$' + product.price
                  td
                    img(src=product.thumbnail width="56")    
        else
          h5(class="alert alert-dark")='Ups :( no se encontrar productos!' 
        hr
        button(type="button" class="btn btn-outline-secondary" onclick="window.location.href='/';") Volver al Formulario
      
      script(src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous")
    
